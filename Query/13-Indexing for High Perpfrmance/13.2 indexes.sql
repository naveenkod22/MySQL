-- 13.2 indexes

-- indexes are type of data structers, used by database engines to quickly find data

-- index are small enough to fit in memory, insted of disk

-- cost of indexes : increase the database, slow down writes

-- design indexes based in your quires, not your tables

-- indexes are stored as binary trees